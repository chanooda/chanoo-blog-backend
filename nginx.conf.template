# /etc/nginx/conf.d/blog_server.conf
# 1. 업스트림 정의 (이름을 blog_server_group으로 지정)
upstream blog_server_group {
    include /etc/nginx/conf.d/blog_server_target.inc;
}

server {
    listen 80;
    server_name api.example.com;

    location / {
        # 2. 위에서 정의한 이름을 그대로 사용
        proxy_pass http://blog_server_group;
        
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}